{% extends 'base-admin.html.twig' %}

{% block title %}All Categories
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<div class="col-lg-12">
				{% for label, messages in app.flashes %}
					{% for message in messages %}
						<div class="alert alert-{{ label }} text-center my-3">
							{{ message }}
						</div>
					{% endfor %}
				{% endfor %}

				<h1 class="page-header my-3 text-center">List of Subcategories</h1>
				<a href="{{ path('app_admin_add_update_subcategory') }}" class="btn btn-success btn-sm mb-3 float-end">Add Subcategory</a>
				<table class="table table-bordered table-striped table-hover">
					<thead>
						<tr>
							<th>ID</th>
							<th>Title</th>
							<th>Description</th>
							<th>Category</th>
							<th>Edit</th>
							<th>Delete</th>
						</tr>
					</thead>
					<tbody>
						{% for subcategory in subcategories %}
						<tr>
							<td>{{ subcategory.id }}</td>
							<td>{{ subcategory.title }}</td>
							<td>{{ subcategory.description }}</td>
							<td>{{ subcategory.category.title }}</td>
							<td>
								<a href="{{ path('app_admin_add_update_subcategory', {id: subcategory.id}) }}" class="btn btn-info btn-sm">Edit</a>
							</td>
							<td>
								<a href="{{ path('app_admin_delete_subcategory', {id: subcategory.id}) }}" class="btn btn-danger btn-sm">Delete</a>
							</td>
						</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
		{# display navigation #}
		<div>
				{{ knp_pagination_render(subcategories) }}
		</div>
	</div>
{% endblock %}